version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "Starting build for CarboCheck web app"
    build:
      commands:
        - echo "No build process needed for static HTML"
    postBuild:
      commands:
        - echo "Build completed"
  artifacts:
    baseDirectory: web
    files:
      - '**/*'
  cache:
    paths: []
